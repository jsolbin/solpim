export interface Like {
  id: string
  userId: string
  artworkId: string
  createdAt: string
}

export interface FavoriteArtist {
  id: string
  userId: string
  artistId: string
  createdAt: string
}
